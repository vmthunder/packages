============
Installation
============

At the command line::

    $ pip install python-voltclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-voltclient
    $ pip install python-voltclient